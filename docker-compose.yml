version: "3.8"
services:
  plexsync:
    build: .
    container_name: plexsync
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_DEBUG=0
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
